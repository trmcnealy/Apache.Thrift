

namespace Apache.Thrift.Database
{
    public enum TAggKind
    {
        AVG                   = 0,
        MIN                   = 1,
        MAX                   = 2,
        SUM                   = 3,
        COUNT                 = 4,
        APPROX_COUNT_DISTINCT = 5,
        SAMPLE                = 6,
    }
}
