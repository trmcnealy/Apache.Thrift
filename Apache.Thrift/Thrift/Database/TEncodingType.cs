

namespace Apache.Thrift.Database
{
    public enum TEncodingType
    {
        NONE         = 0,
        FIXED        = 1,
        RL           = 2,
        DIFF         = 3,
        DICT         = 4,
        SPARSE       = 5,
        GEOINT       = 6,
        DATE_IN_DAYS = 7,
    }
}
